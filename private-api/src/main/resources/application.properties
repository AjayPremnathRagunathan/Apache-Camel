# Bind Spring Boot to all IPv4 interfaces so curl/CLI can access it
server.address=0.0.0.0
server.port=8081

# Application name
spring.application.name=private-api

# Only internal usage (management endpoints)
management.endpoints.web.exposure.include=health,info
